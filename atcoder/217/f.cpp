#include <bits/stdc++.h>

#define ff first
#define ss second
#define ll long long

#define pb(x) push_back(x)
#define pf(x) push_front(x)
#define eb(x) emplace_back(x)
#define ef(x) emplace_front(x)
#define sz(x) int(x.size())

#define max(a, b) (a > b ? a : b)
#define min(a, b) (a < b ? a : b)

#define forn(i, a, b) for(int i = a; i <= b; ++i)
#define ford(i, b, a) for(int i = b; i >= a; --i)

#define DEBUG(X) { auto _X = (X); cout << "L" << __LINE__ << ": " << #X << " = " << (_X) << endl; }
#define PR(A, n) { cerr << "L" << __LINE__ << ": " << #A << " = "; forn(_, 1, n) cerr << A[_] << ' '; cerr << endl; }
#define PR0(A, n) { cerr << "L" << __LINE__ << ": " << #A << " = "; forn(_, 0, n - 1) cerr << A[_] << ' '; cerr << endl; }


int main() {
	int x = 0;
	DEBUG(x);
	return 0;
}
